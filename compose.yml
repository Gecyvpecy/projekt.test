services:
  web:
    build: .
    ports:
      - "8081:8081"
    # Docker Compose automaticky hledá soubor .env v aktuálním adresáři
    # a tyto proměnné předá do kontejneru.
    environment:
      - PORT
      - OPENAI_API_KEY
      - OPENAI_BASE_URL
