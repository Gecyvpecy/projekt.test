services:
  app:
    build: .
    ports:
      # První číslo je port, který uvidíš ty v prohlížeči. 
      # Druhé číslo (vpravo) musí být stejné jako proměnná PORT dole.
      - "8081:8081"
    environment:
      - PORT=8081
